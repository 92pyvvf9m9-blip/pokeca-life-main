Pokeca Life v1.24.3 — stale remote feed cleanup hotfix

Upload or replace all 4 files:

1. package.json
2. remote-feed-core.js   (new file, repository root)
3. collector/tests/remote-feed-prune.test.mjs   (new file)
4. index.html

After upload:
1. Wait for Deploy Pokeca Life to become green.
2. Open the app and reload once.
3. The next automatic sync removes remote lottery records no longer present in lottery-feed.json.
4. If needed, run the app's update/sync once. Do not manually delete individual records.

Safety behavior:
- Local/user-created records are never removed.
- Manual published records are removed only after the manual API sync succeeds.
- An empty or failed base feed cannot erase cached records.
- When one corrected record has the same URL, shop, and deadline, its user status is carried over.
- Ambiguous same-URL records are never auto-merged.
